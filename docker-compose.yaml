services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    env_file:
      - .env

  redis:
    image: redis:alpine
    command: redis-server --requirepass root
    ports:
      - "6379:6379"